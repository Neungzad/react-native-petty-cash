/**
 * @flow
 */

'use strict';

const initialState = {
  
};

const expense = (state = initialState, action) => {
  switch (action.type) {
    case '' :

    case '' :    
  }

 /* if (action.type === 'EXPENSE_CREATE_SUCCESS') {
    return {
     
      }
    };
  }*/

  return state;
}

module.exports = expense;
